# 前端技术栈与设计风格建议（AI 支付产品）

## 一、技术栈建议（优先可上线）

推荐：`Next.js 15 + TypeScript + Tailwind CSS + shadcn/ui + TanStack Query + ECharts`

- **Next.js 15（App Router）**：路由、SSR、部署成熟，适合后台控制台和营销页同仓管理。
- **TypeScript**：支付场景强类型更稳，减少联调错误。
- **Tailwind + shadcn/ui**：开发效率高，组件可控，不会被重主题系统限制。
- **TanStack Query**：处理交易状态轮询、重试、缓存失效很合适。
- **ECharts**：账单趋势、成功率、风控告警图表表现稳定。

## 二、为什么这是 AI 支付场景最优解

- 支付后台需要大量表格、状态、筛选，`shadcn/ui` 非常实用。
- AI 产品常需要“实时感”，`TanStack Query + WebSocket/SSE` 扩展方便。
- 后续若做开发者中心文档页，Next.js 可直接整合（MDX/Docs）。

## 三、设计风格方向（更符合 AI 支付）

关键词：**专业可信 + 轻科技感 + 高可读**

- 视觉基调：深色中性背景 + 高对比强调色（蓝绿系）
- 信息层级：卡片化 + 大数字 KPI + 清晰状态标签
- 交互反馈：支付状态变化用时间轴 + 徽章，不要花哨动画
- 风格边界：避免“Web3 霓虹炫技风”，优先“金融后台可信感”

## 四、建议的设计 Token（可直接用）

- 主色：`#2F6BFF`（Primary Blue）
- 成功：`#12B76A`
- 警告：`#F79009`
- 失败：`#F04438`
- 背景深色：`#0B1220`
- 面板深色：`#111A2B`
- 文本主色：`#E6EDF7`
- 文本次级：`#9FB0CC`

## 五、核心页面结构（MVP 前端）

1. **Dashboard**：余额、今日支出、成功率、失败告警
2. **Agent 管理**：创建 Agent、状态、DID、绑定账户
3. **授权规则**：单笔/日限额、商户白名单
4. **充值页**：充值申请、到账状态、充值流水
5. **交易页**：交易列表、交易详情、错误码、`requestId`
6. **设置页**：API Key、Webhook、签名配置

## 六、交互规范（重点）

- 支付状态必须统一：`INIT / SETTLING / SETTLED / FAILED`
- 所有错误必须显示标准错误码（如 `PAY-003`）
- 所有交易详情显示 `requestId`，便于排障
- 风险拦截提示要给“可操作下一步”（如“去调整限额”）

## 七、工程建议（你现在就可以做）

- 目录建议：
  - `frontend/apps/console`（运营控制台）
  - `frontend/packages/ui`（共享组件）
  - `frontend/packages/api-client`（后端 SDK 封装）
- 首期不做微前端，保持单仓单应用，降低复杂度
- 与后端联调时，优先打通：开户 -> 授权 -> 支付 -> 状态查询
